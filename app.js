async function loadData(){return (await fetch('games.json?v=5', {cache:'no-store'})).json()}
function esc(v=''){return String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
if(document.getElementById('games'))loadData().then(d=>{const a=d.games||[],s=document.getElementById('search'),grid=document.getElementById('games'),count=document.getElementById('count'),empty=document.getElementById('empty');
function render(){let q=s.value.toLowerCase().trim(),f=a.filter(g=>`${g.name} ${g.version} ${g.platform} ${g.category}`.toLowerCase().includes(q));count.textContent=`${f.length} ${f.length===1?'juego':'juegos'}`;empty.classList.toggle('hidden',f.length>0);grid.innerHTML=f.map(g=>`<article class="card"><div class="cover">${g.image?`<img src="${esc(g.image)}" alt="${esc(g.name)} ${esc(g.version)}" loading="lazy" onerror="this.style.display='none'">`:''}<small>${esc(g.platform)}</small></div><div class="card-body"><p class="eyebrow">${esc(g.category)}</p><h3>${esc(g.name)}</h3><div class="version">Versión ${esc(g.version)}</div><p>${esc(g.short)}</p><a class="btn small" href="game.html?id=${encodeURIComponent(g.id)}">Ver información →</a></div></article>`).join('')}s.addEventListener('input',render);render()}).catch(err=>{console.error(err);const grid=document.getElementById('games');const count=document.getElementById('count');if(count)count.textContent='';if(grid)grid.innerHTML='<div class="empty"><h2>No se pudieron cargar los juegos</h2><p>Recarga la página e inténtalo de nuevo.</p></div>'})